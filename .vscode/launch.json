{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "API",
            "url": "http://localhost:3001",
            "webRoot": "${workspaceFolder}"
        }
    ]
}